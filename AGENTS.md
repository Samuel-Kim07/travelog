# Travelog Codex 작업 기준

이 저장소에서 작업하는 Codex 및 자동화 에이전트는 다음 기준을 따른다.

- 기존 기능을 임의로 삭제하거나 변경하지 않는다.
- 요청한 기능과 직접 관련된 파일만 수정한다.
- 작업 전에 관련 HTML, CSS, JavaScript 연결 구조를 확인한다.
- 모바일 화면을 우선으로 검토한다.
- `main` 브랜치에 직접 커밋하거나 푸시하지 않는다.
- 한 작업에는 한 가지 기능만 수정한다.
- 사용되지 않는 것으로 보이는 파일도 사용자 승인 없이 삭제하지 않는다.
- Supabase SQL은 운영 DB 구조를 확인하지 않고 추측해 작성하지 않는다.
- `service_role` 키, GitHub 토큰, DB 비밀번호를 파일에 저장하지 않는다.
- 완료 후 변경된 파일과 테스트 결과를 보고한다.

